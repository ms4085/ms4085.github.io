<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Professional Portfolio | Rutgers Accounting</title>
    <!-- Modern Typography -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <!-- Animation Library -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <style>
        :root {
            --bg-color: #ffffff;
            --text-main: #1a1a1a;
            --text-secondary: #555555;
            --accent-color: #cc0033; /* Rutgers Red accent */
            --section-padding: 100px 10%;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-main);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Smooth Scroll */
        html {
            scroll-behavior: smooth;
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 20px 10%;
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #eee;
        }

        nav a {
            text-decoration: none;
            color: var(--text-main);
            margin-left: 30px;
            font-weight: 400;
            font-size: 0.9rem;
            transition: color 0.3s;
        }

        nav a:hover {
            color: var(--accent-color);
        }

        /* Hero Section */
        header {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 0 10%;
            background: radial-gradient(circle at center, #fafafa 0%, #ffffff 100%);
        }

        .profile-img {
            width: 180px;
            height: 180px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 30px;
            border: 4px solid #fff;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }

        h1 {
            font-size: 3rem;
            font-weight: 600;
            margin-bottom: 10px;
            letter-spacing: -1px;
        }

        .headline {
            font-size: 1.25rem;
            color: var(--text-secondary);
            max-width: 600px;
        }

        /* About Section */
        #about {
            padding: var(--section-padding);
            background: #fdfdfd;
        }

        .section-title {
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 3px;
            color: var(--accent-color);
            margin-bottom: 20px;
            display: block;
        }

        .about-content {
            font-size: 1.5rem;
            max-width: 800px;
            font-weight: 300;
        }

        /* Skills Grid */
        #skills {
            padding: var(--section-padding);
        }

        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
            margin-top: 50px;
        }

        .skill-card {
            padding: 30px;
            background: #fff;
            border: 1px solid #eee;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .skill-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
        }

        .skill-card h3 {
            margin-bottom: 15px;
            font-size: 1.1rem;
        }

        .skill-card p {
            font-size: 0.9rem;
            color: var(--text-secondary);
        }

        /* Portfolio/Resources */
        #resources {
            padding: var(--section-padding);
            background: #f9f9f9;
        }

        .resource-links {
            display: flex;
            gap: 20px;
            margin-top: 30px;
        }

        .btn {
            padding: 12px 25px;
            border: 1px solid var(--text-main);
            text-decoration: none;
            color: var(--text-main);
            font-size: 0.9rem;
            transition: all 0.3s;
        }

        .btn:hover {
            background: var(--text-main);
            color: #fff;
        }

        /* Footer */
        footer {
            padding: 50px 10%;
            border-top: 1px solid #eee;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.8rem;
            color: var(--text-secondary);
        }

        @media (max-width: 768px) {
            h1 { font-size: 2rem; }
            nav { display: none; }
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo"><strong>PORTFOLIO</strong></div>
        <div class="links">
            <a href="#about">About</a>
            <a href="#skills">Skills</a>
            <a href="#resources">Resources</a>
        </div>
    </nav>

    <header>
        <!-- Referencing Assets/ProfilePic.jpeg as per the Master Prompt[cite: 1] -->
        <img src="Assets/ProfilePic.jpeg" alt="Profile Picture" class="profile-img" data-aos="fade-up">
        <h1 data-aos="fade-up" data-aos-delay="100">Emerging Accounting Professional</h1>
        <p class="headline" data-aos="fade-up" data-aos-delay="200">Rutgers Class of 2026 | Bridging Financial Strategy & Data Systems</p>
    </header>

    <section id="about">
        <span class="section-title" data-aos="fade-right">Our Story</span>
        <div class="about-content" data-aos="fade-up">
            <p>As a first-generation college graduate from Rutgers University, I bring a dedicated work ethic and a fresh perspective to the accounting field. Graduating this semester, I am eager to apply my background in financial principles and systems modeling to a forward-thinking professional team.</p>
        </div>
    </section>

    <section id="skills">
        <span class="section-title" data-aos="fade-right">Expertise</span>
        <div class="skills-grid">
            <div class="skill-card" data-aos="zoom-in" data-aos-delay="100">
                <h3>Financial Systems Modeling</h3>
                <p>Specialized in mapping complex business processes and database structures using UML[cite: 1].</p>
            </div>
            <div class="skill-card" data-aos="zoom-in" data-aos-delay="200">
                <h3>Advanced Excel</h3>
                <p>Proficient in data analysis, pivot tables, and leveraging spreadsheets for organizational efficiency[cite: 1].</p>
            </div>
            <div class="skill-card" data-aos="zoom-in" data-aos-delay="300">
                <h3>Bilingual Communication</h3>
                <p>Capable of navigating and communicating effectively in diverse professional environments[cite: 1].</p>
            </div>
            <div class="skill-card" data-aos="zoom-in" data-aos-delay="400">
                <h3>Strategic Organization</h3>
                <p>Focused on meticulous record-keeping and optimizing accounting workflows[cite: 1].</p>
            </div>
        </div>
    </section>

    <section id="resources">
        <span class="section-title" data-aos="fade-right">Professional Assets</span>
        <p>Explore my credentials and media projects below.</p>
        <div class="resource-links" data-aos="fade-up">
            <!-- Placeholders for Assets folder content[cite: 1] -->
            <a href="Assets/Resume.pdf" class="btn" target="_blank">View Resume (PDF)</a>
            <a href="#resources" class="btn">View Video Project</a>
        </div>
    </section>

    <footer>
        <p>&copy; 2026 Rutgers Accounting Portfolio</p>
        <div>
            <a href="#" style="margin-right: 15px; color: inherit; text-decoration: none;">LinkedIn</a>
            <a href="#" style="color: inherit; text-decoration: none;">GitHub</a>
        </div>
    </footer>

    <!-- Animation Scripts -->
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({
            duration: 1000,
            once: true,
            offset: 100
        });
    </script>
</body>
</html>